
zeichen a;
zeichen b[12];
zeichen c;
doppel c;

leer f(ganzzahl a, zeichen t[] );

leer foo(int y[5], int x)
{
  zeichen y[9];

  cout << y;
}

main ()
{
  ganzzahl a;

  temp = b("32");
  temp = temp && 1;
  b[9] = temp;
  b(1,2,3,temp);
  a = ! temp;
  if( a ) goto i10;
  cin >> a;
  cout << "Teste";
i10: return 0;
}
