
ganzzahl f(ganzzahl a, zeichen t) {

};


leer foo(ganzzahl y[5], ganzzahl x)
{
  zeichen y[9];

  cout << y;
}

main ()
{
  ganzzahl a;

  zeichen a;
  zeichen b[12];
  zeichen c;
  doppel c;

  temp = b("32");
  temp = temp && 1;
  b[9] = temp;
  b(1,2,3,temp);
  a = ! temp;
  if( a ) goto i10;
  cin >> a;
  cout << "Teste";
i10: return 0;
}
