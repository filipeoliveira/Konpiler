
schnur concatena(schnur a, schnur b) {
   	obplus (a > b) mach
        { Result = a+b;}
    sonst mach
        { Result = b+a; };
}

main :{

  schnur nome1;
  schnur nome2;

  schreibln("Digite o seu nome: ");
  leseln(nome1);
  schreibln("Digite o seu sobrenome");
  leseln(nome2);

  schreibln("\n");

  schreibln("Bom dia, ");

	concatena(nome1, nome2);

}:
